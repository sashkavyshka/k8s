apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: animal-ingress
spec:
  ingressClassName: nginx
  rules: 
  - http:
      paths:
      - path: /
        pathType: Prefix      
      backend:
          service:
            name: bear-svc
            port:
              number: 80
