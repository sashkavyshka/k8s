apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: animal-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: "52.208.193.80"
    http:
      paths:
      - path: /
        pathType: Exact
        backend:
          service:
            name: moose-svc
            port:
              number: 80     
